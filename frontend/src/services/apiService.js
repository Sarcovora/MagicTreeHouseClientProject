// /src/services/apiService.js

/**
 * This service centralizes all interactions with the backend API.
 * Initially, it will contain hardcoded data/functions to simulate API calls.
 * Later, these functions will be updated to make real requests to the backend
 * (which interacts with Airtable, Supabase, etc.).
 */

// Example structure (will be populated in the next step):
/*
export const getProjects = async () => {
  // TODO: Implement API call or return mock data
  return [];
};

export const getSeasons = async () => {
  // TODO: Implement API call or return mock data
  return [];
};

// ... other API functions
*/

// Placeholder export to avoid empty file issues if any bundler checks exist
const apiService = {};
export default apiService;